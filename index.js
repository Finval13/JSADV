"use strict";

// const div = document.querySelectorAll("img");
// let arr = [];
// div.forEach((elem) => (arr += " ; " + elem.src));
// console.log(arr);

// let arr2 = [];
// div.forEach((elem) => (arr2 += " , " + elem.alt));
// console.log(arr2);

// const div = document.querySelectorAll("div");
// let arr = [];
// div.forEach((elem) => (arr += elem.className + "  , "));

// console.log(arr);

// const img = document.querySelectorAll("img");

// img.forEach((image) => {
//   const imss = image.getAttribute("Alt");
//   if (imss) {
//     image.setAttribute("title", imss);
//   }
// });

// const div = document.querySelectorAll(".back");
// div.forEach((elem) => (elem.style.backgroundColor = "#fff000"));
